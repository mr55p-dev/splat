# yaml-language-server: $schema=schema.json
---
name: example-dev
container:
  image: example
  tag: latest
nginx:
  server-name: dev.example.com
  container-port: 8080
volumes:
  - name: data
    source: "data"
